Switch to Python3 first

after git clone, cd into folder

pip install virtualenv

python -m virtualenv env

source ./env/bin/activate

pip install -r requirements.txt

cd demo

python manage.py migrate

python manage.py runserver 0.0.0.0:8000 --settings=demo.settings

http://localhost:8000/
